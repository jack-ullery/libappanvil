Index: b/security/apparmor/lsm.c
===================================================================
--- a/security/apparmor/lsm.c
+++ b/security/apparmor/lsm.c
@@ -507,7 +507,7 @@ static inline int aa_mmap(struct file *f
 	struct dentry *dentry;
 	int mask = 0;
 
-	if (!file)
+	if (!file || !file->f_security)
 		return 0;
 
 	if (prot & PROT_READ)
