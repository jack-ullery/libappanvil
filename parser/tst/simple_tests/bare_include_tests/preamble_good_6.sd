#=DESCRIPTION old includes - keyword include if exists of non-existent directory in preamble
#=EXRESULT PASS

include if exists <includes-preamble-does-not-exist>

/bin/foo {
  /usr/some/other/thing r,
}
